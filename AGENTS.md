# AGENTS.md

本文件为在本仓库中工作的 AI 编码代理提供约定和上下文。默认以中文沟通，除非用户明确要求使用其他语言。

## 项目概览

禅道 PMS 是一个基于 PHP 的开源项目管理系统，采用自研框架和模块化 MVC 架构。核心业务覆盖产品、项目、执行、任务、缺陷、测试、文档、组织、报表和 DevOps 集成等场景。

修改代码前先根据需求定位模块，优先沿用现有模块、控制器、模型、语言包、视图和 UI 组件的组织方式，不要为局部变更引入新的框架或大范围重构。

## 目录速览

- `framework/`：核心框架代码，包括控制器、模型、路由、帮助函数等基础能力。
- `module/`：业务模块目录。典型模块包含 `control.php`、`model.php`、`zen.php`、`tao.php`、`config/`、`lang/`、`view/`、`ui/`、`css/`、`js/`。
- `config/`：全局配置。运行环境的本地配置通常写在未跟踪的 `config/my.php`。
- `db/`：数据库建表、升级和初始化 SQL。
- `www/`：Web 入口和公共静态资源。
- `lib/`：第三方库和通用库。
- `extension/`：扩展机制和自定义扩展。
- `test/`：项目自定义测试框架、测试数据和 API 测试。
- `misc/`：兼容性检查、构建辅助、迁移辅助等脚本。
- `bin/`、`roadrunner/`：命令行和 RoadRunner 相关脚本。

## 常用命令

在运行耗时命令前先确认本地环境是否已配置数据库、PHP 扩展和 Web 服务。

```bash
make all       # 完整构建
make clean     # 清理构建产物
make common    # 构建核心通用包
make package   # 打包
make pms       # 生成开源版发布包
make ci        # CI 构建
```

测试相关：

```bash
cd test
php spider.php
```

前端资源压缩或兼容性检查可参考 `misc/` 下脚本，例如 `misc/compatibility/check.sh`。不要在没有必要时运行全量构建或全量测试；窄范围改动优先运行相关模块或相关脚本。

## 编码约定

- 遵守 `.editorconfig`：默认 4 空格缩进、LF、UTF-8、文件末尾保留换行；`Makefile` 使用 Tab；YAML 使用 2 空格。
- 保持 PHP 5.6+ 兼容性意识。不要随意使用较新 PHP 语法，除非相邻代码已明确使用且目标版本允许。
- 遵循现有模块风格：控制器放在 `control.php`，业务和数据逻辑放在 `model.php`、`zen.php` 或 `tao.php`，视图放在 `view/` 或 `ui/`。
- 文案必须走语言包，优先修改对应模块的 `lang/{locale}.php`，避免在视图或控制器中硬编码用户可见文本。
- 数据库访问优先使用项目 DAO 链式 API 和 `TABLE_*` 常量，避免拼接 SQL 字符串。
- 变更数据库结构时同步考虑升级 SQL、初始化 SQL、表常量和测试数据。
- 修改前端时优先沿用模块已有 `js/`、`css/`、`ui/` 组织和 ZUI/jQuery 风格。
- 保持变更范围小。不要顺手格式化无关文件，不要改动用户未要求的历史代码。

## 安全与数据处理

- 所有外部输入都要按现有模式过滤、校验和授权检查。
- 关注 XSS、CSRF、越权访问、SQL 注入、文件上传和路径遍历风险。
- 输出到页面的用户数据应使用项目既有转义方式。
- 不要提交真实密钥、数据库配置、会话数据、上传文件或 `tmp/` 中的临时内容。
- 本地环境配置应放在 `config/my.php` 等未跟踪文件中。

## 国际化

本项目支持多语言。涉及用户可见文案时至少检查简体中文和英文语言文件；如果相邻功能已有繁体、德语、法语等语言项，尽量保持键名一致并补齐对应文件。新增语言键应使用模块内已有命名习惯。

## 测试与验证

提交或交付前，根据变更范围选择验证方式：

- PHP 语法改动：对改过的 PHP 文件运行 `php -l path/to/file.php`。
- 模块业务逻辑：优先查找并运行 `test/` 或模块目录下相关测试。
- API 改动：检查 `test/api/` 下相关用例。
- 数据库改动：验证安装库、升级脚本和表常量是否一致。
- 前端改动：检查相关页面在目标语言和常见浏览器尺寸下无明显布局问题。

如果由于环境缺失无法运行测试，在回复中明确说明未验证的项目和原因。

## Git 工作区规则

- 工作区可能已有用户改动。不要回滚、覆盖或清理不是自己产生的改动。
- 修改前后可用 `git status --short` 判断影响范围。
- 不要执行 `git reset --hard`、`git checkout -- <file>` 等破坏性命令，除非用户明确要求。
- 生成构建产物或缓存后，确认是否应保留；无关产物不要纳入变更。

## 与用户沟通

- 默认使用中文，保持简洁、直接、可执行。
- 说明已做的改动、验证过的命令，以及未能验证的原因。
- 遇到需求不明确时，优先从仓库现有模式推断并继续；只有在合理假设会带来明显风险时才暂停询问。
