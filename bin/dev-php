#!/usr/bin/env bash
set -euo pipefail

PHP_BIN="${PHP_BIN:-$HOME/.phpbrew/php/php-8.2.31/bin/php}"

if [[ ! -x "$PHP_BIN" ]]; then
  echo "PHP not found or not executable: $PHP_BIN" >&2
  exit 127
fi

exec "$PHP_BIN" "$@"
